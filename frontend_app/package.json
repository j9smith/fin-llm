{
  "scripts": {
    "frontend:start": "cd goose && npm start",
    "frontend:dev": "cd goose && npm run dev",

    "backend:up": "docker compose up -d fin-llm-backend",
    "backend:down": "docker compose down",

    "start": "concurrently -k \"npm run backend:up\" \"npm run frontend:start\""
  },
  "dependencies": {
    "dompurify": "^3.1.7",
    "marked": "^14.1.4",
    "serve": "^14.2.4"
  }
}
